class SbotServicePlaceholder {
  Future<String> processQuery(String query) async {
     await Future.delayed(const Duration(seconds: 1));
     return "Respuesta generada (placeholder): pronto S-BOT podrá darte información real basada en Panamá.";
  }
}
