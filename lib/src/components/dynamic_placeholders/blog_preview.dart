import 'package:flutter/material.dart';

class BlogPreview extends StatelessWidget {
  const BlogPreview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
