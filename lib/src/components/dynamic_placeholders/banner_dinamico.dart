import 'package:flutter/material.dart';

class BannerDinamico extends StatelessWidget {
  const BannerDinamico({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
